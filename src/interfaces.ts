// Definição da interface ITask que descreve o formato de uma tarefa
export interface ITask {
    taskName: string; // O nome da tarefa
    deadline: number; // O prazo para concluir a tarefa, em dias
}
